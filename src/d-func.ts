export const dFunc = () => {
  // This is a placeholder function that does nothing.
  // It can be used as a default value for optional callbacks. We use this to avoid default value for callbacks () => {}, which
  // will cause unnecessary re-renders.
};
